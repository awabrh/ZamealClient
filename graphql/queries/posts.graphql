query Posts($location: String) {
  posts(location: $location) {
    id
    createdAt
    updatedAt
    imageId
    carModel
    numberOfSeats
    isAcWorking
    locations
    price
    departure
    arrival
    days
    user {
      id
      name
      dep
      batch
      address
    }
  }
}
