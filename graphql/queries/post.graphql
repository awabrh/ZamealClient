query Post($id: Float!) {
  post(id: $id) {
    id
    createdAt
    updatedAt
    carModel
    numberOfSeats
    isAcWorking
    locations
    price
    departure
    arrival
    days
    user {
      id
      name
      dep
      batch
      mobile
      address
    }
  }
}
